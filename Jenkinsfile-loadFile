def map = [:]
def modules = [:]

pipeline {
  agent any

  stages {
    stage('load file') {
      steps {
        script {
          modules.lib = load "./jenkins/lib/shared.groovy"
          modules.lib.getSelf()
        }
      }
    }
  }
}
