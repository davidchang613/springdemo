pipeline {
    agent none
    
    stages{
        stage('build spring project') {
            agent {
                label 'agent2'
            }
            
        }
        stage('Docker Build') {
            agent any
            steps {
                sh "docker version"
                sh "docker build . -f Dockerfile-build -t davidchang613/springdemo:${VERSION}"
            }
        }
        
    }
}
